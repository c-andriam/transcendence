meta {
  name: User favorites desc sorted by cookTime
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}{{myFavPrefix}}?page=2&limit=5&sortBy=cookTime&sortOrder=desc
  body: none
  auth: bearer
}

params:query {
  page: 2
  limit: 5
  sortBy: cookTime
  sortOrder: desc
}

headers {
  x-gateway-api-key: {{gatewayKey}}
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
