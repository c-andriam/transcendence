meta {
  name: Delete from favorite
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}{{favPrefix}}
  body: none
  auth: bearer
}

headers {
  x-gateway-api-key: {{gatewayKey}}
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
