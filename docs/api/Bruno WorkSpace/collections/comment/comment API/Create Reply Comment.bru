meta {
  name: Create Reply Comment
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}{{commentPrefix}}/{{commentId}}/replies
  body: json
  auth: bearer
}

headers {
  x-gateway-api-key: {{gatewayKey}}
  Content-Type: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
      "content": "Merci !"
  }
}

script:post-response {
  bru.setGlobalEnvVar("replyComId", res.body.data.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
