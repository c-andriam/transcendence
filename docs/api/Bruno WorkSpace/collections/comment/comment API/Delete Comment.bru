meta {
  name: Delete Comment
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}{{commentPrefix}}/{{commentId}}
  body: none
  auth: bearer
}

headers {
  x-gateway-api-key: {{gatewayKey}}
  ~Content-Type: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
      "content": "Cette recette est délicieuse ! Merci pour le partage."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
