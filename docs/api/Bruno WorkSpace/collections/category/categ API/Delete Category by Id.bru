meta {
  name: Delete Category by Id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}{{categPrefix}}/{{aperiCatId}}
  body: none
  auth: bearer
}

headers {
  x-gateway-api-key: {{gatewayKey}}
  ~Content-Type: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "name": "Apéritifs",
    "iconName": "wine-glass",
    "color": "#FF6B6B",
    "sortOrder": 1
  }
}

assert {
  res.body.data: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
