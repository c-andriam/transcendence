meta {
  name: Delete Recipe Rate by Id
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}{{ratingPrefix}}
  body: json
  auth: bearer
}

headers {
  x-gateway-api-key: {{gatewayKey}}
  Content-Type: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "userId": "{{userId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
